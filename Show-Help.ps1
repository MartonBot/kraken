
$helpEntries = @{
    "kraken help"="Get a help message"
    "kraken init"="Create a stub octopus-project.json file"
    "kraken status"="Get a summary of the project's basic details and variable templates"
    "kraken substitutions"="List all the Octopus substitutions found in configuration files"
    "kraken templates"="Show details of the variable templates locally and on the server"
    "kraken update"="Update the local variable so that all substitutions are associated with a variable template"
    "kraken pull project"="Populates the octopus-project.json file with metadata from the server"
    "kraken pull templates"="If some templates exist on the server but not locally, copy them to the project file"
    "kraken pull values"="Overwrite local tenant values with the server version"
    "kraken push templates"="Overwrite server templates with the local version"
    "kraken push values"="Overwrite tenant values with the local version"
}

$helpEntries